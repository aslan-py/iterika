"""Тексты промптов для LLM-классификатора.

Этот модуль намеренно освобождён от лимита длины строк (см.
pyproject.toml → per-file-ignores), чтобы правила классификации
можно было редактировать как обычный текст, не разбивая строки.
Меняйте _CLASSIFICATION_RULES, чтобы поменять логику сегментации.
"""

_SYSTEM = (
    'Ты — аналитик ценовых сегментов товаров на маркетплейсах. '
    'Определяй сегмент каждого товара: Эконом, Стандарт или Премиум.'
)

# ── Правила классификации ─────────────────────────────────────────
# Редактируйте здесь, чтобы изменить логику присвоения сегментов
# без правки кода функции _build_prompt.
_CLASSIFICATION_RULES = (
    'Правила классификации (ПО ПРИОРИТЕТУ):\n'
    '\n'
    '1. ОСНОВА — сегмент определяется ценой ОТНОСИТЕЛЬНО категории:\n'
    '   - Эконом: цена значительно ниже среднего по этой категории\n'
    '   - Стандарт: цена в районе среднего по категории\n'
    '   - Премиум: цена значительно выше среднего по категории\n'
    '\n'
    '2. ПОДЪЁМ В ПРЕМИУМ по ПРИЗНАКАМ (ДОПОЛНИТЕЛЬНО к цене):\n'
    '   Товар МОЖЕТ быть Премиум ДАЖЕ при низкой цене, ЕСЛИ:\n'
    '   - Это топовая модель известного бренда ( к примеру iPhone Pro/Max, Galaxy Ultra/Fold)\n'
    '   - Это люксовый/нишевый бренд (мировая узнаваемость, высокая репутация)\n'
    '   - Бренд ассоциируется с дорогим сегментом (парфюм, часы, дизайнерская одежда)\n'
    '   Если товар не брендовый  не на слуху НИКОГДА не ставь премиум , даже если цена  высокая\n'
    '\n'
    '3. НЕ ПОДНИМАЙ в Премиум, ЕСЛИ:\n'
    '   - Это масс-маркет бренд (PARISA, MIXIT, EVELINE, LUXVISAGE, Fito cosmetic, VOIS)\n'
    '   - Это бюджетная линейка (Redmi, POCO, Galaxy A/M, realme C)\n'
    '   - Бренд не известен широко (безымянные, малоизвестные марки)\n'
    '   - В названии есть: "Lite", "A", "M", "C", "Eco", "Basic"\n'
    '   — для них решает ТОЛЬКО цена, без исключений\n'
    '\n'
    '4. Если сомневаешься — ставь СТАНДАРТ'
)

# Шаблон промпта. Плейсхолдеры подставляются в _build_prompt.
# Литеральные фигурные скобки JSON экранированы удвоением ({{ }}).
_PROMPT_TEMPLATE = (
    'Категория: {category}.\n'
    'Ценовые границы сегментов (по данной выборке):\n'
    '  Эконом:    до {p_low:.0f} руб\n'
    '  Стандарт:  {p_low:.0f} – {p_high:.0f} руб\n'
    '  Премиум:   от {p_high:.0f} руб\n\n'
    '{rules}\n\n'
    'Товары:\n{body}\n\n'
    'Ответь JSON: {{"segments": [...]}} — '
    'ровно {count} строк в том же порядке.\n'
    'Допустимые значения: Эконом, Стандарт, Премиум.'
)
