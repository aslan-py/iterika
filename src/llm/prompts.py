"""Тексты промптов для LLM-классификатора.

Этот модуль намеренно освобождён от лимита длины строк (см.
pyproject.toml → per-file-ignores), чтобы правила классификации
можно было редактировать как обычный текст, не разбивая строки.
Меняйте _CLASSIFICATION_RULES, чтобы поменять логику сегментации.
"""

_SYSTEM = (
    'Ты — аналитик товаров на маркетплейсах. Определяй сегмент каждого '
    'товара: Эконом, Стандарт или Премиум. Сегмент определяется брендом '
    'и узнаваемостью товара, а не только его ценой.'
)

# ── Правила классификации ─────────────────────────────────────────
# Редактируйте здесь, чтобы изменить логику присвоения сегментов
# без правки кода функции _build_prompt.
_CLASSIFICATION_RULES = (
    'ГЛАВНОЕ ПРАВИЛО: Премиум — это про БРЕНД, а не про цену.\n'
    'Высокая цена сама по себе НИКОГДА не делает товар Премиумом.\n'
    'Если бренд не на слуху, безымянный или малоизвестный — это НИКОГДА\n'
    'не Премиум, какой бы высокой ни была цена.\n'
    '\n'
    'Правила (по приоритету):\n'
    '\n'
    '1. ПРЕМИУМ — только если бренд действительно премиальный:\n'
    '   - Топовая модель известного бренда (например iPhone Pro/Max, Galaxy Ultra/Fold)\n'
    '   - Люксовый/нишевый бренд мировой узнаваемости и высокой репутации\n'
    '   - Бренд из дорогого сегмента (дизайнерский парфюм, швейцарские часы, люкс-одежда)\n'
    '\n'
    '2. НЕ ПРЕМИУМ (масс-маркет, бюджет, безымянные) — делятся на Эконом/Стандарт по цене:\n'
    '   - Масс-маркет бренды (например PARISA, MIXIT, EVELINE, LUXVISAGE, Fito cosmetic, VOIS)\n'
    '   - Бюджетные линейки (Redmi, POCO, Galaxy A/M, realme C)\n'
    '   - Безымянные и малоизвестные марки\n'
    '   - В названии есть "Lite", "A", "M", "C", "Eco", "Basic"\n'
    '   Для них: Эконом — дешёвые, Стандарт — средние и дороже.\n'
    '\n'
    '3. Если сомневаешься в бренде — это НЕ Премиум, ставь Стандарт или Эконом.'
)

# Шаблон промпта. Плейсхолдеры подставляются в _build_prompt.
# Литеральные фигурные скобки JSON экранированы удвоением ({{ }}).
_PROMPT_TEMPLATE = (
    'Категория: {category}.\n\n'
    '{rules}\n\n'
    'Товары (name — название, brand — бренд, price_rub — цена):\n'
    '{body}\n\n'
    'Ответь JSON: {{"segments": [...]}} — '
    'ровно {count} строк в том же порядке.\n'
    'Допустимые значения: Эконом, Стандарт, Премиум.'
)
